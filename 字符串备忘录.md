## 字符串备忘录

### KMP

$KMP$ 的 $nex$ 数组中如果**末尾**跳转距离是总长的倍数说明 $str[ nex[len] +1,len]$ 是这段字符串的最小表达串/最小循环串,即最小表达串的长度为 $len-nex[len]$
此处最小表达串eg. $ABCABCABCABCABC$ 则最小表达串为 $ABC$

### 哈希

对于哈希求解 **最小循环串** 可以通过枚举长度的最小质因子来求解 [P3538](https://www.luogu.com.cn/problem/P3538)
```cpp
void Moblus() {
    for (int i = 2; i <= MAXN; i++) {
        if (!check[i]) {
            g[i] = i;
        }
        for (int j = 0; j < tot; j++) {
            if (i * prime[j] > MAXN)
                break;
            g[i * prime[j]] = prime[j];// 最小质因子
        }
    }
}
ll len = y - x + 1; // x,y表示取得字符串s[x,y]
if (h.same(h, x, y - 1, x + 1, y)) {
    cout << 1 << '\n';
}
ll ans = y - x + 1;
while (len > 1) {
    if (h.same(h, x + ans / g[len], y, x, y - ans / g[len])) {
        ans /= g[len];
    }
    len /= g[len];
}
cout  << ans;
```

